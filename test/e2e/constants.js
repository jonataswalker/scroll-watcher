export const windowSize = { w: 600, h: 400 };

// from CSS
export const dragSize = { w: 140, h: 80 };

export const dragPosition = { top: 100, left: 100 };

// also from CSS
export const betweenRect = 200;

export const dragClasses = {
    enter: 'enter',
    partialExit: 'partial-exit',
    fullyEnter: 'fully-enter',
    exit: 'exit',
};
